# =================================================================
# HEXAA CLINIC VOICE AGENT - ENVIRONMENT VARIABLES
# =================================================================
# Copy this file to .env.local for local development
# For Railway deployment, add these as Service Variables in Dashboard
# =================================================================

# -----------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------
# PostgreSQL connection string with asyncpg driver
# Format: postgresql+asyncpg://user:password@host:port/database
# Railway: Use the DATABASE_URL from your PostgreSQL plugin
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database

# -----------------------------------------------------------------
# LIVEKIT CONFIGURATION (Voice/Phone Agent)
# -----------------------------------------------------------------
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# -----------------------------------------------------------------
# AI SERVICE API KEYS
# -----------------------------------------------------------------
# OpenAI - for GPT-4o-mini LLM
OPENAI_API_KEY=sk-your_openai_api_key

# ElevenLabs - for Text-to-Speech
ELEVEN_LABS=your_elevenlabs_api_key

# Deepgram - for Speech-to-Text (recommended for phone quality)
DEEPGRAM_API_KEY=your_deepgram_api_key

# -----------------------------------------------------------------
# GOOGLE CALENDAR INTEGRATION
# -----------------------------------------------------------------
# Option 1: Path to service account JSON file (local development)
GOOGLE_SERVICE_ACCOUNT_FILE=google_service_account.json

# Option 2: JSON content directly (RECOMMENDED for Railway/cloud)
# Paste the entire service account JSON as a single line
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Calendar ID (usually the clinic's Gmail address)
GOOGLE_CALENDAR_ID=your_calendar_id@gmail.com

# -----------------------------------------------------------------
# RAILWAY-SPECIFIC (Auto-injected by Railway)
# -----------------------------------------------------------------
# PORT - Railway automatically sets this
# RAILWAY_ENVIRONMENT - Current environment name
# RAILWAY_SERVICE_NAME - Name of your service
# =================================================================